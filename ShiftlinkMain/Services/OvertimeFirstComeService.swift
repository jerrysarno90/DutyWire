import Foundation

/// Legacy placeholder to satisfy the Xcode project references that were tied to
/// the old rotation-based overtime engine. The new overtime workflow relies on
/// ManagedOvertimeViewModel inside ContentView instead.
@available(*, deprecated, message: "Use ManagedOvertimeViewModel instead")
enum OvertimeFirstComeService {}
