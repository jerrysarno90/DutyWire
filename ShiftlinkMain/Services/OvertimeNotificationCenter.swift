import Foundation

/// Deprecated stub kept so the build system has a matching source file for the
/// legacy overtime notification helper. Notifications for postings are now handled
/// directly inside the ManagedOvertime flow.
@available(*, deprecated, message: "Replaced by ManagedOvertime workflow")
enum OvertimeNotificationCenter {}
