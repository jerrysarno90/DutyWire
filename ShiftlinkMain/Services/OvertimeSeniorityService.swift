import Foundation

/// Stub service retained only so that existing Xcode targets still build while
/// the new managed overtime pipeline fully replaces the rotation-era helpers.
@available(*, deprecated, message: "Use ManagedOvertimeViewModel instead")
enum OvertimeSeniorityService {}
